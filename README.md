# Urent Parser

Набор Python скриптов для загрузки и конвертации данных из API сервиса аренды самокатов и велосипедов [Urent](https://urentbike.ru/). Позволяет получать информацию о зонах аренды, ограничениях и доступном транспорте в формате GeoJSON.

## 🎯 Возможности

- 🗺️ **Загрузка зон аренды** (rent zones) для всех городов или конкретного города
- 🚦 **Загрузка зон ограничений** (restriction zones): лоуспиды, запреты парковки, запреты поездок
- 🌍 **Загрузка границ городов** (city boundaries) присутствия сервиса
- 📦 **Конвертация в GeoJSON** для использования в картографических приложениях
- 🔄 **Автоматическое обновление** данных при устаревании

## 🚀 Быстрый старт

### Установка зависимостей

```bash
pip install requests
```

### Базовое использование

```bash
# 1. Скопировать пример конфигурации
cp config.json.example config.json

# 2. Добавить Bearer токен в config.json
# См. раздел "Получение токена" ниже

# 3. Загрузить все зоны ограничений
python3 fetch_all_restrictions.py

# 4. Загрузить зоны аренды
python3 fetch_all_zones.py

# 5. Загрузить границы городов
python3 convert_cities_to_geojson.py
```

## 📁 Структура проекта

```
.
├── fetch_all_restrictions.py              # ⭐ Загрузка всех restriction zones
├── fetch_all_zones.py                     # Загрузка всех rent zones
├── fetch_city_zones.py                    # Загрузка rent zones одного города
├── convert_cities_to_geojson.py           # Загрузка и конвертация city boundaries
├── convert_restriction_zones_to_geojson.py # Конвертер restriction zones из JSON
│
├── config.json.example                    # Шаблон конфигурации
├── .gitignore                             # Защита токена
├── README.md                              # Документация проекта
├── API.md                                 # Документация API Urent
│
├── examples/                              # Примеры ответов API
└── output/                                # Выходные GeoJSON файлы
```

## 📚 Скрипты

### 1. Загрузка всех зон ограничений (рекомендуется)

```bash
python3 fetch_all_restrictions.py
```

**Что делает:**
- Загружает список всех городов
- Для каждого AVAILABLE города загружает rent zones
- Для каждой rent zone загружает restriction zones (лоуспиды, запреты)
- Сохраняет в `output/all_restrictions.geojson`

⏱️ **Время выполнения:** 5-10 минут (~100+ запросов)

**Результат:** Один файл со всеми зонами ограничений, каждая зона имеет:
- `type`: `lowSpeedZone`, `restrictedZone` или `notAllowedZone`
- `rentZoneId`: ID родительской rent zone
- `speedLimitValue`: значение ограничения скорости (для lowSpeedZone)

---

### 2. Загрузка зон аренды

```bash
python3 fetch_all_zones.py
```

**Что делает:**
- Загружает список городов
- Для каждого AVAILABLE города загружает rent zones
- Сохраняет в `output/all_rent_zones.geojson`

⏱️ **Время выполнения:** 1-2 минуты (~21 город)

---

### 3. Загрузка зон одного города

```bash
python3 fetch_city_zones.py <city_id> [output_name]
```

**Примеры:**
```bash
# Майкоп
python3 fetch_city_zones.py 64307422f158b50245fdae7d maykop

# Краснодар
python3 fetch_city_zones.py 5f96dd383719ad000142ba5a krasnodar
```

**Результат:** 
- `{название}.json` - сырой ответ API
- `{название}.geojson` - сконвертированные rent zones

---

### 4. Загрузка границ городов

```bash
python3 convert_cities_to_geojson.py
```

**Что делает:**
- Проверяет наличие `output/cities.json`
- Если файла нет или он старше 5 дней - загружает из API
- Конвертирует в GeoJSON

**Результат:**
- `output/all_zones.geojson` - только AVAILABLE города (~36)
- `output/all_zones_including_frozen.geojson` - все города (~355)

---

### 5. Конвертер restriction zones

```bash
python3 convert_restriction_zones_to_geojson.py <input.json> [output.geojson]
```

Для ручной конвертации сохранённых JSON файлов с restriction zones.

## 🔐 Получение токена

### Способ 1: Через файл конфигурации (рекомендуется)

1. Скопируйте шаблон:
```bash
cp config.json.example config.json
```

2. Отредактируйте `config.json`:
```json
{
  "bearer_token": "ваш_токен_здесь"
}
```

### Способ 2: Через переменную окружения

```bash
export URENT_TOKEN="ваш_токен"
python3 fetch_all_zones.py
```

### Получение токена из приложения

1. Установите инструмент перехвата трафика:
   - **iOS/macOS**: [Charles Proxy](https://www.charlesproxy.com/), [Proxyman](https://proxyman.io/)
   - **Android**: [HTTP Toolkit](https://httptoolkit.tech/), Charles Proxy
   - **Универсально**: [mitmproxy](https://mitmproxy.org/)

2. Настройте перехват HTTPS трафика
3. Откройте приложение Urent и сделайте любой запрос (например, откройте карту)
4. Найдите запрос к `backyard.urentbike.ru`
5. Скопируйте значение заголовка `Authorization: Bearer ...`

**⚠️ Важно:**
- Токены действительны несколько часов
- При ошибке 403 (Forbidden) обновите токен
- Файл `config.json` в `.gitignore` - не коммитьте токены!

## 📊 Формат данных

### GeoJSON

Все скрипты создают стандартные [GeoJSON](https://geojson.org/) файлы:

```json
{
  "type": "FeatureCollection",
  "features": [
    {
      "type": "Feature",
      "id": "zone_id",
      "properties": {
        "id": "zone_id",
        "name": "Название зоны",
        "type": "lowSpeedZone",
        "rentZoneId": "parent_rent_zone_id",
        "speedLimitValue": 15
      },
      "geometry": {
        "type": "Polygon",
        "coordinates": [[[lng, lat], [lng, lat], ...]]
      }
    }
  ]
}
```

**⚠️ Важно:** Координаты в GeoJSON имеют формат `[долгота, широта]` (не `[широта, долгота]`)!

### Типы зон

Проект работает с тремя типами зон:

1. **RENT ZONES** - зоны аренды (где можно брать/оставлять транспорт)
2. **RESTRICTION ZONES** - зоны ограничений:
   - `lowSpeedZones` - ограничение скорости (10-15 км/ч)
   - `restrictedZones` - запрет парковки
   - `notAllowedZones` - запрет поездок
3. **CITY BOUNDARIES** - границы городов присутствия сервиса

Подробнее см. [API.md](API.md)

## 💡 Полезные команды

```bash
# Посмотреть количество зон в GeoJSON
python3 -c "import json; data=json.load(open('output/all_restrictions.geojson')); print(len(data['features']))"

# Валидировать GeoJSON онлайн
# https://geojson.io или https://geojsonlint.com

# Красиво отформатировать JSON
python3 -m json.tool input.json > output_formatted.json

# Проверить статус файлов
ls -lh output/
```

## 📖 Документация

- **[API.md](API.md)** - полная документация API Urent
- **[examples/](examples/)** - примеры ответов API

## 🔒 Безопасность

⚠️ **Важно:** 
- Файл `config.json` содержит токен авторизации
- Он добавлен в `.gitignore` и не должен попасть в git
- **Никогда не коммитьте токены в публичные репозитории!**

## 📝 Зависимости

- Python 3.7+
- requests

```bash
pip install requests
```

## 🤝 Вклад

Проект открыт для улучшений! Если нашли баг или хотите добавить функциональность - создавайте Issue или Pull Request.

## ⚖️ Лицензия

Этот проект создан в образовательных целях. Используйте API Urent ответственно и в соответствии с их условиями использования.

## 🔗 Ссылки

- [Официальный сайт Urent](https://urentbike.ru/)
- [Приложение iOS](https://apps.apple.com/ru/app/urent/id1458778465)
- [Приложение Android](https://play.google.com/store/apps/details?id=ru.urentbike.app)
