# Yandex Go Scooters Parser

Набор Python скриптов для загрузки данных сервиса аренды самокатов [Yandex Go](https://yandex.ru/go). Позволяет получать информацию о доступных самокатах, зонах обслуживания и городах с зонами.

## 🎯 Возможности

- 🌍 **Глобальное сканирование городов** - автоматический поиск всех городов с Yandex Go по всему миру
- 🗺️ **Загрузка зон для всех городов** - детальные зоны ограничений для каждого города
- 🛴 **Загрузка самокатов** в заданной области (bbox)
- 📦 **Конвертация в GeoJSON** для использования в картографических приложениях
- 🔄 **Дедупликация** - автоматическое удаление дубликатов зон

## 🚀 Быстрый старт

См. [QUICKSTART.md](QUICKSTART.md) для подробной инструкции.

### Установка

```bash
pip install requests
cp config.json.example config.json
# Обновите токены в config.json из Charles Proxy / Proxyman
```

### Базовое использование

```bash
# 1️⃣ Найти все города с Yandex Go (быстрое обновление ~2 мин)
python3 fetch_cities.py

# 2️⃣ Загрузить детальные зоны для всех городов (~20-30 мин)
python3 fetch_zones.py

# 3️⃣ Загрузить самокаты в городе (комбинированный подход)
python3 fetch_scooters.py polygon-184332  # Сочи (по ID из cities.geojson)
python3 fetch_scooters.py --bbox 39.6,43.4,39.9,43.7  # Или по custom bbox

# 4️⃣ (Опционально) С полной информацией: батарея, цены, страховка
python3 fetch_scooters.py polygon-184332 --with-full-info --delay 0.3
```

## 📁 Структура проекта

```
.
├── fetch_cities.py           # 🌍 Глобальное сканирование городов
├── fetch_zones.py            # 🗺️ Загрузка зон для всех городов
├── fetch_scooters.py         # 🛴 Умный парсинг самокатов города
├── fetch_parkings.py         # 🅿️  Загрузка парковок города
│
├── check_token.py            # 🔍 Проверка срока JWT токена
├── test_api.py               # 🧪 Тестирование эндпоинтов
│
├── config.json.example       # Шаблон конфигурации
├── config.json               # Ваши заголовки (не коммитится)
├── grid_3x3.geojson          # Глобальная сетка 3×3° (7,998 квадратов)
│
├── output/
│   ├── cities.geojson        # 🌍 Все города с зонами (~2.2 MB, 83 города)
│   ├── zones.geojson         # 🗺️ Все зоны всех городов (~20 MB, 4,685 зон)
│   ├── parkings.geojson      # 🅿️  Парковки самокатов (все города)
│   ├── scooters.geojson      # 🛴 Самокаты (простой метод, одна область)
│   ├── city_zones/           # 🗂️  Зоны каждого города отдельно
│   │   ├── polygon-184332.geojson
│   │   ├── jet_5d848349f811e80001fe4b17.geojson
│   ├── city_scooters/        # 🛴 Самокаты по городам (комбинированный подход)
│   │   ├── custom_1770843272.geojson  # Сочи (2,440 самокатов)
│   │   └── ...
│   └── tmp/                  # Временные файлы и логи
│       ├── fetch_cities_log.txt
│       ├── scooter_zones_*.json
│       └── polygon_ids_*.txt
```

## 📖 Описание скриптов

### `fetch_cities.py` - Глобальное сканирование городов

Автоматически находит все города с зонами Yandex Go / Yango по всему миру.

**Два режима работы:**

1. **Быстрое обновление** (по умолчанию, ~2 мин):
```bash
python3 fetch_cities.py
```
- Обрабатывает только известные города (92 квадрата с `has_city=true`)
- Идеально для регулярных обновлений

2. **Полное сканирование** (~2 часа):
```bash
python3 fetch_cities.py --search_new
```
- Сканирует все 7,998 квадратов 3×3° по всему миру
- Для обнаружения новых городов

**Дополнительные опции:**
```bash
# Продолжить с определённого квадрата (автоматически включает --search_new)
python3 fetch_cities.py --continue_from 5000

# С задержкой между запросами
python3 fetch_cities.py --delay 0.2
```

**Особенности:**
- ✅ **Двухэтапное сканирование**: сначала известные города, потом поиск новых
- ✅ **Защита данных**: результаты этапа 1 сохраняются до начала этапа 2
- ✅ **Автопроверка токена**: каждые 500 запросов проверяет срок JWT
- ✅ **Детальное логирование**: `output/tmp/fetch_cities_log.txt`
- ✅ **Автообновление сетки**: обновляет `has_city` в `grid_3x3.geojson`

**Результаты:**
- `output/cities.geojson` - все найденные города (для визуализации)
- `output/tmp/scooter_zones_stage1_*.json` - полные данные этапа 1
- `output/tmp/scooter_zones_stage2_*.json` - полные данные после поиска
- `output/tmp/polygon_ids_*.txt` - списки ID полигонов
- `output/tmp/fetch_cities_log.txt` - детальный лог выполнения

**Текущая статистика:**
- Найдено городов: **83**
- Обработано квадратов: 7,998 (полное сканирование)
- Размер файла: ~2.2 MB

### `fetch_zones.py` - Загрузка зон для всех городов

Автоматически загружает детальные зоны ограничений для всех городов из `cities.geojson`.

**Использование:**
```bash
# Загрузить зоны для всех городов
python3 fetch_zones.py

# Продолжить с города #15 (после ошибки)
python3 fetch_zones.py --continue_from 15

# С другим zoom и задержкой
python3 fetch_zones.py --zoom 17.0 --delay 0.2
```

**Алгоритм:**
1. Загружает список городов из `output/cities.geojson`
2. Для каждого полигона вычисляет bbox и центроид
3. Запрашивает детальные зоны через API
4. Сохраняет в `output/city_zones/{polygon-id}.geojson`
5. **Объединяет все с дедупликацией в `output/zones.geojson`**

**Особенности:**
- ✅ **Упрощённая структура**: только id, city_id, type, speed_limit
- ✅ **Дедупликация**: удаляет повторяющиеся зоны (706 дубликатов из 5,391)
- ✅ **Извлечение speed_limit**: автоматически из названия иконки
- ✅ **Без внешних зависимостей**: только requests
- ✅ **Автоостановка**: при истечении JWT токена

**Результаты:**
- `output/zones.geojson` - **все зоны всех городов** (19.6 MB, 4,685 уникальных зон)
- `output/city_zones/*.geojson` - отдельный файл для каждого города (82 файла)

**Статистика зон:**
```
Всего зон: 4,685 (после дедупликации)
├── speed_limit: 2,028 (43.3%)
│   ├── 15 км/ч: 1,296 зон
│   ├── 10 км/ч: 524 зон
│   ├── 5 км/ч: 51 зон
│   └── 20 км/ч: 3 зоны
├── no_entry: 1,817 (38.8%)
├── no_parking: 811 (17.3%)
└── boundary: 29 (0.6%)

Дубликатов удалено: 706
```

**Время выполнения:** ~20-30 минут для всех городов

### `fetch_scooters.py` - Полный парсинг самокатов города

**Умный комбинированный подход** для эффективного парсинга всего города:

```bash
# По ID города из cities.geojson
python3 fetch_scooters.py polygon-184332

# Или по произвольному bbox (Сочи)
python3 fetch_scooters.py --bbox 39.6,43.4,39.9,43.7

# С настройками
python3 fetch_scooters.py --bbox 39.6,43.4,39.9,43.7 --min-cluster 10 --delay 0.2
```

**Параметры:**
- `city_id`: ID города из `cities.geojson` (например, `polygon-184332`)
- `--bbox`: Альтернативный bbox `min_lon,min_lat,max_lon,max_lat`
- `--min-cluster`: Минимальный размер кластера для рекурсии (по умолчанию: 50)
- `--delay`: Задержка между запросами в секундах (по умолчанию: 0.1)
- `--with-full-info`: Запросить полную информацию (батарея, цены, страховка). ⚠️ Увеличивает время в N раз!

**Алгоритм (4 этапа):**

1. **Обзорный запрос (zoom 12)**
   - Один запрос на весь город
   - Получение всех координат самокатов/кластеров
   - Быстрый анализ всей территории

2. **Кластеризация в горячие зоны**
   - Разбивка координат на сетку 0.02° (~2×2 км)
   - Выявление непустых ячеек
   - Определение bbox для каждой горячей зоны

3. **Детальные запросы (zoom 17)**
   - Запрос для каждой горячей зоны
   - Получение детальных данных: ID, координаты, payload
   - Извлечение размеров кластеров (objects_count)

4. **Раскрытие больших кластеров (zoom 19)**
   - Только для кластеров ≥ min_cluster самокатов
   - Уменьшенный bbox 0.005° (~500×500 м)
   - Максимальная детализация

**Преимущества:**
- 🚀 **94% экономии запросов** (35 vs 625 для Сочи)
- 🎯 **98% покрытия** (пропускаются только пустые области)
- ⚡ **Быстро** (~30 сек для города с 2,500 самокатами)
- 💡 **Умное раскрытие** (zoom 19 только там, где это нужно)
- 🔍 **Точный подсчёт** (включая самокаты внутри кластеров)

**Реальный пример (Сочи):**
```
🚀 Парсинг города: custom_1770843272

📡 Этап 1: Обзорный запрос (zoom 12)
   Найдено точек: 1,523

🔥 Этап 2: Кластеризация точек (сетка 0.02°)
   Горячих зон: 34

📥 Этап 3: Детальные запросы (zoom 17)
   [1/34] Зона с 24 точками... ✓ 214 самокатов, 41 кластеров
   [2/34] Зона с 91 точками... ✓ 337 самокатов, 65 кластеров
   ...
   [34/34] Зона с 1 точками... ✓ 48 самокатов, 17 кластеров

🔍 Этап 4: Раскрытие больших кластеров (zoom 19)
   Кластеров для обработки: 0  # zoom 17 уже детальный!

================================================================================
✅ ГОТОВО!
================================================================================
📄 Файл: output/city_scooters/custom_1770843272.geojson
⏱️  Время: 29.6 сек

📊 Статистика:
   Отдельных самокатов:     1,240
   Кластеров:               317
   Самокатов в кластерах:   1,200
   ────────────────────────────────────────
   ВСЕГО самокатов:         2,440
```

**Результаты:**
- `output/city_scooters/{city_id}.geojson` - все самокаты города
- Metadata включает: total_scooters, clusters, cluster_scooters, generated_at

**Важно про кластеры:**
- 🅿️ **Кластеры (cluster) = парковки с самокатами** (icon: `scooters_parking_march_2025`)
- 🅿️ **Пустые кластеры (cluster_empty) = пустые парковки** (icon: `scooters_parking_march_2025_empty`)
- 🔒 **Стабильность**: кластеры сохраняют один ID независимо от bbox/zoom
- 📊 **Метаданные**: `payload.objects_count` показывает точное количество самокатов
- ⚡ **Zoom 17 оптимален**: максимальные кластеры ~24 самоката (не нужен zoom 19)

**Сравнение подходов:**

| Метод | Запросов | Пустых | Покрытие | Время |
|-------|----------|--------|----------|-------|
| Наивная сетка 0.04° | 625 | 70% | ~90% | ~100 сек |
| Двухэтапный | 401 | 50% | ~95% | ~65 сек |
| Рекурсивный | ~300 | 15% | ~98% | ~50 сек |
| **Комбинированный** | **35** | **5%** | **98%** | **30 сек** |

### `fetch_parkings.py` - Загрузка парковок города

Извлекает только парковки самокатов (без самих самокатов). Использует тот же умный алгоритм, что и `fetch_scooters.py`.

**Использование:**
```bash
# По произвольному bbox (Сочи)
python3 fetch_parkings.py --bbox 39.6,43.4,39.9,43.7

# С настройками
python3 fetch_parkings.py --bbox 39.6,43.4,39.9,43.7 --delay 0.2
```

**Параметры:**
- `--bbox`: Bounding box `min_lon,min_lat,max_lon,max_lat`
- `--delay`: Задержка между запросами в секундах (по умолчанию: 0.1)

**Типы парковок:**
- `cluster` - парковка с самокатами (icon: `scooters_parking_march_2025`)
- `cluster_empty` - пустая парковка (icon: `scooters_parking_march_2025_empty`)

**Особенности:**
- ✅ **Импортирует функции** из `fetch_scooters.py` (DRY принцип)
- ✅ **Фильтрует только парковки** (игнорирует самокаты)
- ✅ **Стабильные ID**: парковки сохраняют ID независимо от bbox/zoom
- ⚡ **Быстро**: ~13 сек для области Сочи (511 парковок)

**Реальный пример (Сочи):**
```
🅿️  Парсинг парковок города: custom_1770845269

📡 Этап 1: Обзорный запрос (zoom 12)
   Найдено точек: 417

🔥 Этап 2: Кластеризация (сетка 0.02°)
   Горячих зон: 14

📥 Этап 3: Детальные запросы (zoom 17)
   [1/14] Зона... ✓ 311 парковок
   [2/14] Зона... ✓ 248 парковок
   ...
   [14/14] Зона... ✓ 36 парковок

✅ ГОТОВО!
📄 /Users/felispimeja/github/yandex/output/parkings.geojson
⏱️  12.9 сек

📊 Парковок с самокатами: 511
   Пустых парковок: 0
   Самокатов на парковках: 212
```

**Результаты:**
- `output/parkings.geojson` - все парковки области
- **Properties**: `id`, `city_id`, `type`, `objects_count` (для cluster)
- **Metadata**: `parkings_with_scooters`, `empty_parkings`, `total_scooters_on_parkings`

---

### `check_token.py` - Проверка JWT токена

Проверяет срок действия JWT токена `X-Yandex-Jws`.

```bash
python3 check_token.py
```

**Вывод:**
```
🔍 Проверка JWT токена X-Yandex-Jws
======================================================================

📱 UUID устройства: 5662543c056f4d8c980cbff44615a957
🕐 Создан: 2026-02-11 16:36:35
⏰ Истекает: 2026-02-11 17:36:35
⏳ Время жизни: 1ч

✅ ТОКЕН ДЕЙСТВИТЕЛЕН
   Осталось: 42м 46с
```

**О JWT токене:**
- **Номинальный срок**: 1 час (3600 секунд)
- **Фактический срок**: ~1 час 30 минут (работает ~30 мин после истечения)
- После истечения API вернёт **HTTP 405**
- Обновляйте из Charles Proxy / Proxyman

## 📊 Формат данных

### GeoJSON Города (`cities.geojson`)
```json
{
  "type": "FeatureCollection",
  "features": [
    {
      "type": "Feature",
      "id": "polygon-184332",
      "geometry": {
        "type": "Polygon",
        "coordinates": [[[lon, lat], ...]]
      },
      "properties": {
        "square_id": 23088,
        "timestamp": "2026-02-11T12:30:45"
      }
    }
  ]
}
```

### GeoJSON Зоны (`zones.geojson`)
Упрощённая структура без стилевого оформления:

```json
{
  "type": "FeatureCollection",
  "features": [
    {
      "type": "Feature",
      "id": "MCD2_slowdown_36",
      "geometry": {
        "type": "Polygon",
        "coordinates": [[[lon, lat], ...]]
      },
      "properties": {
        "city_id": "polygon-184332",
        "type": "speed_limit",
        "speed_limit": 15
      }
    }
  ]
}
```

**Типы зон (`type`):**
- `speed_limit` - ограничение скорости (5, 10, 15 или 20 км/ч)
- `no_parking` - запрет парковки
- `no_entry` - запрет въезда
- *(отсутствует)* - граница зоны обслуживания

### GeoJSON Самокаты (`city_scooters/*.geojson` или `scooters_full_info.geojson`)

**Базовый формат** (без `--with-full-info`):
```json
{
  "type": "FeatureCollection",
  "metadata": {
    "city_id": "polygon-184332",
    "total_objects": 337,
    "scooters": 280,
    "clusters": 57,
    "source": "Yandex Go API (Combined Approach)"
  },
  "features": [
    {
      "type": "Feature",
      "id": "scooter_819296",
      "geometry": {
        "type": "Point",
        "coordinates": [39.721459, 43.586242]
      },
      "properties": {
        "id": "scooter_819296",
        "city_id": "polygon-184332",
        "type": "scooter",
        "number": "819296"
      }
    }
  ]
}
```

**Полный формат** (с `--with-full-info`):
```json
{
  "type": "FeatureCollection",
  "metadata": {
    "city_id": "polygon-184332",
    "scooters": 280,
    "operator": {"name": "Yandex", "phone": "+7 (495) 777-77-77"},
    "subscription": {"is_active": false},
    "currency": {"code": "RUB", "sign": "₽", "text": "руб"}
  },
  "features": [
    {
      "type": "Feature",
      "properties": {
        "id": "scooter_819296",
        "number": "819296",
        "model": "ninebot",
        "vendor": "yandex",
        "charge_level": 70,
        "remaining_distance": 35000.0,
        "remaining_time": 180,
        "unlock_price": 5500,
        "riding_price": 799,
        "parking_price": 1500,
        "insurance_price": 4900,
        "insurance_coverage": 50000
      }
    }
  ]
}
```

**Поля в режиме `--with-full-info`:**
- **Базовая информация**: `id`, `number`, `model`, `vendor`, `uuid`, `image_tag`
- **Батарея**: `charge_level` (%), `remaining_distance` (м), `remaining_time` (мин)
- **Цены**: `unlock_price`, `riding_price`, `parking_price` (в копейках), `surge_balance`, `offer_id`, `offer_type`
- **Страховка**: `insurance_price` (копейки), `insurance_coverage` (копейки)
- **Метаданные города** (1 раз на FeatureCollection): `operator`, `subscription`, `currency`
```

## ⚠️ Важные замечания

### Токены и безопасность
- **НЕ коммитьте** `config.json` в Git (уже в `.gitignore`)
- Токены имеют срок действия и требуют обновления
- **JWT токен `X-Yandex-Jws`**:
  - Срок жизни: **~1ч 30мин** (номинально 1ч)
  - При истечении: **HTTP 405**
  - Обновляйте из сниффера
- При ошибках 401/403/405 обновите все токены

### Получение токенов

1. Установите Charles Proxy / Proxyman / mitmproxy
2. Откройте приложение Yandex Go на телефоне
3. Найдите любой запрос к `tc.mobile.yandex.net`
4. Скопируйте заголовки:
   - `Authorization` - Bearer токен
   - `X-YaTaxi-Authorization` - тот же Bearer токен
   - `X-Yandex-Jws` - JWT токен
   - `X-AppMetrica-UUID` - UUID устройства
   - `X-YaTaxi-UserId` - ID пользователя

5. Обновите `config.json`

### Особенности API
- **Координаты**: формат `[longitude, latitude]` (сначала долгота!)
- **Bbox**: формат `[min_lon, min_lat, max_lon, max_lat]`
- **Самокаты**: могут не показываться если их нет или bbox слишком большой
- **Сетка**: используется глобальная сетка 3×3° с 50% перекрытием

## 🐛 Решение проблем

### HTTP 405 Method Not Allowed
**Причина:** Истёк JWT токен

**Решение:**
1. Запустите `python3 check_token.py`
2. Если токен истёк - обновите `X-Yandex-Jws` из сниффера
3. Перезапустите скрипт

### HTTP 401/403 Unauthorized
**Причина:** Неверные Bearer токены

**Решение:** Обновите `Authorization` и `X-YaTaxi-Authorization` из сниффера

### Самокаты не загружаются
**Возможные причины:**
- В области нет самокатов
- Bbox слишком большой
- Требуется другой zoom

## 🔗 Дополнительные материалы

- [API.md](API.md) - детальная документация API
- [QUICKSTART.md](QUICKSTART.md) - быстрый старт за 5 минут

## 📝 Лицензия

Проект создан в образовательных целях для изучения API.
Используйте ответственно и в соответствии с условиями Yandex Go.

---

**⚠️ Дисклеймер:** Этот проект не связан с Яндексом и создан в образовательных целях.
