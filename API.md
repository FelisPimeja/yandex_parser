# Urent API Documentation

Документация по API сервиса аренды самокатов и велосипедов Urent.

## 🗺️ Типы зон в API

### 1. **RENT ZONES (Зоны аренды)** 
Полигоны, определяющие области где можно брать и оставлять транспорт.
- **API endpoint**: `/zones/rent?cityId=XXX`
- **Структура**: `{ id, name, center, coordinates[] }`
- **Назначение**: Показывают где доступна аренда транспорта

### 2. **RESTRICTION ZONES (Зоны ограничений)**
Специальные зоны с ограничениями внутри rent zones.
- **API endpoint**: `/zones/general?rentZoneId=XXX`
- **Типы**:
  - `lowSpeedZones` - зоны с ограничением скорости (10-15 км/ч)
  - `restrictedZones` - зоны запрета парковки
  - `notAllowedZones` - зоны запрета поездок
- **Структура**: `{ id, name, center, coordinates[], speedLimitValue, scheduleType }`

### 3. **CITY BOUNDARIES (Границы городов)**
Административные границы городов присутствия сервиса.
- **API endpoint**: `/zones/uses`
- **Структура**: `{ id, cityId, cityAvailabilityStatus, coordinates[] }`
- **Назначение**: Определяют в каких городах работает сервис

---

**⚠️ ВАЖНО**: Не путать эти три типа зон между собой!

---

## 🔍 Обнаруженные API эндпоинты

### Методология обнаружения
1. **Мониторинг трафика приложения** через Charles Proxy/Proxyman
2. **Автоматическое тестирование** распространённых паттернов API
3. **Анализ версий API** (v1, v3, v5, v6)

### ✅ Рабочие эндпоинты (9/14 протестированных)

#### 1. `/v1/cities` - Полный список городов
- **Размер**: ~66 KB
- **Записей**: 284 города (21 AVAILABLE, 263 FROZEN)
- **Использование**: Получить все города, включая замороженные
```json
{
  "entries": [{
    "id": "682b37b60d6446c972cc333b",
    "location": {"lat": 53.7156, "lng": 91.4292},
    "name": "Абакан",
    "availabilityStatus": "FROZEN",
    "dailyPassEnabled": false
  }]
}
```

#### 2. `/v1/zones/uses` - Активные зоны с деталями
- **Размер**: ~48 KB
- **Записей**: 36 активных зон
- **Использование**: Получить детальную информацию (названия, скорости, транспорт)
- **Отличие от `/v1/cities`**: Только активные зоны, но с богатыми данными

#### 3. `/v1/profile` - Профиль пользователя
- **Размер**: ~1.2 KB
- **Данные**: ФИО, email, телефон, статистика поездок, верификация
```json
{
  "statistics": {
    "distance": 2816.33,      // метры
    "kcal": 61.96,
    "elapsedSeconds": 1126.56,
    "tripCount": 3
  },
  "verified": true,
  "userRating": 5.0
}
```

#### 4. `/v1/profile/referral` - Реферальная программа
- **Размер**: ~254 B
- **Данные**: Реферальный код, количество приглашений, бонусы

#### 5. `/v1/profile/goals` - Персональные цели
- **Размер**: ~43 B
- **Данные**: Список целей пользователя (если настроены)

#### 6. `/v3/places/my` - Избранные места
- **Размер**: ~1.5 KB
- **Данные**: Сохранённые адреса (дом, работа и т.д.)

#### 7. `/v1/payment/acquiring` - Платёжная система
- **Размер**: ~273 B
- **Данные**: Провайдер платежей, publicId, валюта

#### 8. `/v1/profile/cards/with-pendings` - Банковские карты
- **Размер**: ~368 B
- **Данные**: Привязанные карты, холдирования, маски номеров

#### 9. `/v1/daily-pass` - Дневной абонемент
- **Размер**: ~43 B
- **Данные**: Информация о подписке на день

### ❌ Недоступные эндпоинты (404)
- `/v3/rides/active` - Активные поездки (требуется активная сессия)
- `/v3/rides/history` - История поездок
- `/v1/tariffs` - Тарифы
- `/v1/profile/notifications` - Уведомления
- `/v3/promo-banners` - Промо-баннеры

---

## 📊 API Endpoints

### Список городов (City Boundaries)

```
GET https://backyard.urentbike.ru/gatewayclient/api/v3/zones/uses
```

**Параметры:**
- `availableCityTypes`: массив статусов [`available`, `frozen`]
- `locationLat`: широта (например: `55.77545546986907`)
- `locationLng`: долгота (например: `37.63290022965542`)

**Пример запроса:**
```bash
curl "https://backyard.urentbike.ru/gatewayclient/api/v3/zones/uses?availableCityTypes=available&availableCityTypes=frozen&locationLat=55.77545546986907&locationLng=37.63290022965542" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

**Структура ответа:**
```json
{
  "data": [
    {
      "id": "zone_id",
      "cityId": "city_id",
      "cityAvailabilityStatus": "AVAILABLE",
      "coordinates": [
        { "lat": 44.609764, "lng": 40.100516 },
        { "lat": 44.610000, "lng": 40.101000 }
      ],
      "center": { "lat": 44.609764, "lng": 40.100516 },
      "modalities": ["Scooter", "Bike"]
    }
  ],
  "errors": [],
  "succeeded": true
}
```

---

### Зоны аренды города (Rent Zones)

```
GET https://backyard.urentbike.ru/gatewayclient/api/v3/zones/rent
```

**Параметры:**
- `cityId`: ID города (из `/zones/uses`)
- `locationLat`: широта
- `locationLng`: долгота
- `useZoneId`: ID города (дублирует cityId)

**Пример запроса:**
```bash
curl "https://backyard.urentbike.ru/gatewayclient/api/v3/zones/rent?cityId=64307422f158b50245fdae7d&locationLat=55.77545546986907&locationLng=37.63290022965542&useZoneId=64307422f158b50245fdae7d" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

**Структура ответа:**
```json
{
  "data": [
    {
      "id": "rent_zone_id",
      "name": "Название зоны",
      "center": { "lat": 44.609764, "lng": 40.100516 },
      "coordinates": [
        { "lat": 44.609764, "lng": 40.100516 },
        { "lat": 44.610000, "lng": 40.101000 }
      ]
    }
  ],
  "errors": [],
  "succeeded": true
}
```

---

### Зоны ограничений (Restriction Zones)

```
GET https://backyard.urentbike.ru/gatewayclient/api/v5/zones/general
```

**Параметры:**
- `rentZoneId`: ID зоны аренды (из `/zones/rent`)
- `locationLat`: широта
- `locationLng`: долгота

**Пример запроса:**
```bash
curl "https://backyard.urentbike.ru/gatewayclient/api/v5/zones/general?rentZoneId=5d848349f811e80001fe4b17&locationLat=55.77545546986907&locationLng=37.63290022965542" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

**Структура ответа:**
```json
{
  "data": {
    "lowSpeedZones": [
      {
        "id": "restriction_id",
        "name": "ул. Горького, лоуспид 10/км",
        "scheduleType": "DEFAULT",
        "speedLimitValue": 10,
        "coordinates": [
          { "lat": 43.58332959139722, "lng": 39.719805010582014 }
        ],
        "center": { "lat": 43.58313170082143, "lng": 39.72026393513735 }
      }
    ],
    "restrictedZones": [
      {
        "id": "restriction_id",
        "name": "Штрафная зона Запрет парковки",
        "coordinates": [...],
        "center": { "lat": 43.xxx, "lng": 39.xxx }
      }
    ],
    "notAllowedZones": [
      {
        "id": "restriction_id",
        "name": "Лесопарк Запрет поездок",
        "coordinates": [...],
        "center": { "lat": 43.xxx, "lng": 39.xxx }
      }
    ]
  },
  "errors": [],
  "succeeded": true
}
```

---

### Доступный транспорт и парковки

```
GET https://backyard.urentbike.ru/gatewayclient/api/v6/transports
```

**Параметры:**
- `latitude`: широта центра поиска
- `longitude`: долгота центра поиска
- `locationLat`: широта местоположения пользователя
- `locationLng`: долгота местоположения пользователя
- `radiusByMeters`: радиус поиска в метрах
- `withEBikes`: включить велосипеды (`true`/`false`)
- `zoom`: уровень зума карты (11-15)
- `includeEmptyParkings`: включить пустые парковки (`true`/`false`)

**⚠️ ВАЖНО**: API имеет лимит в **250 транспортных средств** на запрос!
При достижении лимита сервер приоритизирует ближайшие объекты.

**Примеры запросов:**

```bash
# Получить транспорт в радиусе 1 км от центра Сочи
curl "https://backyard.urentbike.ru/gatewayclient/api/v6/transports?includeEmptyParkings=true&latitude=43.585525&longitude=39.723062&locationLat=43.585525&locationLng=39.723062&radiusByMeters=1000&withEBikes=true&zoom=14" \
  -H "Authorization: Bearer YOUR_TOKEN"

# Получить транспорт в радиусе 50 км
curl "https://backyard.urentbike.ru/gatewayclient/api/v6/transports?includeEmptyParkings=true&latitude=43.585525&longitude=39.723062&locationLat=43.585525&locationLng=39.723062&radiusByMeters=50000&withEBikes=true&zoom=11" \
  -H "Authorization: Bearer YOUR_TOKEN"
```

**Структура ответа:**

```json
{
  "entries": {
    "parkings": [
      {
        "id": "parking_id",
        "isEmpty": false,
        "countBikes": 0,
        "countScooters": 6,
        "location": { "lat": 43.604, "long": 39.732 },
        "radius": 15
      }
    ],
    "transports": [
      {
        "batteryPercent": 75,
        "identifier": "S.614787",
        "displayedIdentifier": "614-787",
        "type": "Scooter",
        "location": { "lat": 43.597, "long": 39.719 }
      }
    ]
  },
  "useZoneId": "608bc0e3e158803bfd7b93b3",
  "useZoneIds": ["608bc0e3e158803bfd7b93b3"],
  "errors": [],
  "succeeded": true
}
```

**Типы транспорта:**
- `Scooter` - электросамокат
- `EBike` - электровелосипед
- `Bike` - обычный велосипед

---

## 🔬 Особенности работы API транспорта

### Лимит на количество объектов

API `/transports` возвращает **максимум 250 транспортных средств** за один запрос, независимо от радиуса поиска.

**Эксперименты с разными радиусами (Сочи, центр города):**

| Радиус | Самокаты | Велосипеды | Всего | Соотношение велосипедов |
|--------|----------|------------|-------|-------------------------|
| 1 км   | 93       | 42         | 135   | 31.1%                   |
| ~12 км | 189      | 61         | 250   | 24.4%                   |
| 100 км | 220      | 30         | 250   | 12.0%                   |

**Выводы:**

1. **Лимит 250 единиц** срабатывает при радиусе ≥12 км
2. **Приоритизация:** При достижении лимита сервер отдаёт предпочтение самокатам
3. **Для полных данных** по большому городу нужны множественные запросы с меньшими радиусами
4. **Плотность в центре:** ~135 единиц транспорта на радиус 1 км (очень высокая)

### Рекомендации по использованию

**Для получения полной картины транспорта в городе:**

1. Разбейте город на сетку с радиусом 500-1000м для каждой точки
2. Используйте параметр `zoom=14` для детализированных данных
3. Указывайте одинаковые `latitude/longitude` и `locationLat/locationLng` для локального поиска
4. Учитывайте время суток - количество транспорта меняется

**Оптимальные параметры для разных задач:**

```bash
# Быстрый обзор города (один запрос)
radiusByMeters=50000&zoom=11

# Детальный анализ района (точные данные)
radiusByMeters=1000&zoom=14

# Баланс между скоростью и точностью
radiusByMeters=5000&zoom=13
```

---

## 🔐 Авторизация

Все запросы к API требуют Bearer токен в заголовке `Authorization`.

**Заголовки запроса:**
```
Host: backyard.urentbike.ru
User-Agent: Urent/1.89.0 (ru.urentbike.app; build:8; iOS)
Authorization: Bearer YOUR_TOKEN_HERE
Content-Type: application/json
Accept: */*
UR-Client-Id: mobile.client.ios
UR-Platform: iOS
```

### Как получить токен

1. Откройте приложение Urent на iOS/Android
2. Используйте инструмент перехвата трафика:
   - **iOS/macOS**: Charles Proxy, Proxyman
   - **Android**: HTTP Toolkit, Charles Proxy
   - **Универсально**: mitmproxy
3. Сделайте любой запрос в приложении (например, откройте карту)
4. Найдите запрос к `backyard.urentbike.ru`
5. Скопируйте значение заголовка `Authorization: Bearer ...`

**⚠️ Примечания:**
- Токены обычно действительны несколько часов
- При ошибке 403 (Forbidden) обновите токен
- Никогда не публикуйте токены в открытом доступе

---

## 📝 Форматы данных

### Координаты

**В ответах API:**
```json
{
  "lat": 43.585525,
  "lng": 39.723062
}
```

**В GeoJSON (обратный порядок!):**
```json
[39.723062, 43.585525]
```

**⚠️ Важно:** Координаты в GeoJSON имеют формат `[долгота, широта]` (а не `[широта, долгота]`)!

### Статусы городов

- `AVAILABLE` - город активен, доступна аренда
- `FROZEN` - город заморожен, аренда недоступна

### Типы модальностей

- `Scooter` - электросамокаты
- `EBike` - электровелосипеды
- `Bike` - обычные велосипеды
