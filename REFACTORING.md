# Рефакторинг скриптов - Февраль 2026

## 🎯 Цели рефакторинга

1. **Упростить архитектуру** - убрать дублирование кода
2. **Централизовать данные** - единое место для временных файлов
3. **Улучшить переиспользование** - скрипты вызывают друг друга
4. **Добавить новую функциональность** - парковки и транспорт

## 🔄 Что изменилось

### Старая архитектура (удалена)

```
convert_cities_to_geojson.py        - Загрузка и конвертация городов
convert_restriction_zones_to_geojson.py - Ручная конвертация restriction zones
fetch_all_restrictions.py           - Загрузка всех restriction zones
fetch_all_zones.py                  - Загрузка всех rent zones
fetch_city_zones.py                 - Загрузка rent zones одного города
```

**Проблемы:**
- Дублирование кода загрузки токенов
- Дублирование кода конвертации GeoJSON
- Нет единого места для временных данных
- Сложная логика вызовов
- Отсутствие функциональности загрузки транспорта

### Новая архитектура

```
fetch_cities.py     - Загрузка списка городов (базовый модуль)
fetch_zones.py      - Загрузка rent zones + restriction zones
fetch_parkings.py   - Загрузка парковок и транспорта
```

**Преимущества:**
- ✅ Единый модуль для работы с городами (`fetch_cities.py`)
- ✅ Переиспользование через `subprocess` вызовы
- ✅ Централизованное хранение в `output/tmp/`
- ✅ Флаг `--noexport` для гибкости
- ✅ Новая функциональность: парковки и транспорт
- ✅ Меньше кода: 909 строк добавлено, 1321 удалено

## 📊 Сравнение функциональности

| Старые скрипты | Новые скрипты | Улучшения |
|----------------|---------------|-----------|
| `convert_cities_to_geojson.py` | `fetch_cities.py` | + флаг `--noexport`, + централизация |
| `fetch_all_zones.py` | `fetch_zones.py` | + restriction zones, + переиспользование |
| `fetch_all_restrictions.py` | `fetch_zones.py` | Объединено с rent zones |
| `fetch_city_zones.py` | `fetch_zones.py` | Можно фильтровать по городу |
| ❌ Нет | `fetch_parkings.py` | 🆕 Новая функциональность |

## 🗂️ Структура данных

### output/tmp/ (временные JSON, не в git)

```
cities.json                    - Список городов
rent_zones_{city_id}.json      - Rent zones для города
restrictions_{zone_id}.json    - Restriction zones для rent zone
transports_{zone_id}.json      - Транспорт для rent zone
all_zones.json                 - Объединённые данные зон
all_parkings.json              - Объединённые данные парковок
```

### output/ (GeoJSON файлы, в git)

```
cities.geojson          - Границы AVAILABLE городов
cities_all.geojson      - Все города (включая FROZEN)
zones.geojson           - Все зоны (rent + restriction)
parkings.geojson        - Парковки
vehicles.geojson        - Доступный транспорт
```

## 🔧 API использования

### fetch_cities.py

```bash
# Загрузить и сконвертировать в GeoJSON
python3 fetch_cities.py

# Только загрузить JSON (для использования другими скриптами)
python3 fetch_cities.py --noexport
```

**Выход:**
- `output/tmp/cities.json` - сырые данные
- `output/cities.geojson` - AVAILABLE города
- `output/cities_all.geojson` - все города

### fetch_zones.py

```bash
python3 fetch_zones.py
```

**Зависимости:**
- Автоматически вызывает `fetch_cities.py --noexport` если нужно

**Выход:**
- `output/tmp/rent_zones_{city_id}.json` - для каждого города
- `output/tmp/restrictions_{zone_id}.json` - для каждой rent zone
- `output/tmp/all_zones.json` - объединённые данные
- `output/zones.geojson` - все зоны в GeoJSON

### fetch_parkings.py

```bash
python3 fetch_parkings.py
```

**Зависимости:**
- Автоматически вызывает `fetch_cities.py --noexport` если нужно

**Логика:**
1. Загружает список городов
2. Для каждого города загружает rent zones
3. Для каждой rent zone:
   - Вычисляет центр полигона
   - Делает запрос транспорта с радиусом 10км
   - Сохраняет результат

**Выход:**
- `output/tmp/transports_{zone_id}.json` - для каждой rent zone
- `output/tmp/all_parkings.json` - объединённые данные
- `output/parkings.geojson` - парковки
- `output/vehicles.geojson` - транспорт

## 🔒 Безопасность

### .gitignore

Добавлено:
```
output/tmp/        # Временные JSON (могут содержать чувствительные данные)
old_scripts/       # Устаревшие скрипты (не нужны в репозитории)
```

Уже было:
```
config.json        # Токены авторизации
```

## 📝 Миграция для пользователей

Если вы использовали старые скрипты:

```bash
# Старый способ
python3 fetch_all_restrictions.py

# Новый способ
python3 fetch_zones.py
```

```bash
# Старый способ
python3 convert_cities_to_geojson.py

# Новый способ
python3 fetch_cities.py
```

```bash
# Новая функциональность
python3 fetch_parkings.py
```

## ⚠️ Известные ограничения

### fetch_parkings.py

**Проблема:** Радиус 10км от центра rent zone может не покрывать все парковки и транспорт.

**Почему:** API `/transports` требует координаты центра и радиус. Большие rent zones могут иметь транспорт за пределами 10км от центра.

**Решение в будущем:**
- Вычислять несколько точек покрытия для больших зон
- Использовать bounding box вместо радиуса
- Делать несколько запросов с перекрытием

**Текущий статус:** Это первая версия (draft), работает для большинства случаев.

## 📈 Статистика

### Изменения кода

```
10 files changed
909 insertions(+)
1321 deletions(-)
```

**Чистое уменьшение:** -412 строк кода при добавлении новой функциональности!

### Производительность

| Скрипт | Время | Запросов |
|--------|-------|----------|
| `fetch_cities.py` | ~1 сек | 1 |
| `fetch_zones.py` | 5-10 мин | ~100+ |
| `fetch_parkings.py` | 3-5 мин | ~50+ |

### Покрытие данных

- **Города:** ~355 (36 AVAILABLE)
- **Rent zones:** ~50-100 (зависит от городов)
- **Restriction zones:** ~500+ (все типы)
- **Парковки:** ~200+ (приблизительно)
- **Транспорт:** 250 единиц максимум за запрос (ограничение API)

## 🚀 Будущие улучшения

1. **Покрытие парковок:**
   - Множественные точки запросов для больших зон
   - Автоматическое определение оптимального радиуса

2. **Кэширование:**
   - Проверка свежести данных
   - Инкрементальные обновления
   - TTL для разных типов данных

3. **Параллелизм:**
   - Асинхронные запросы (aiohttp)
   - Пул воркеров для обработки

4. **Фильтрация:**
   - Флаги для выбора конкретных городов
   - Фильтры по типам зон
   - Выборочная загрузка

5. **Мониторинг:**
   - Progress bar (tqdm)
   - Детальная статистика
   - Логирование в файл

## 🔗 Ссылки

- [README.md](README.md) - Полная документация
- [API.md](API.md) - Документация API
- [QUICKSTART.md](QUICKSTART.md) - Быстрый старт
- [GitHub](https://github.com/FelisPimeja/urent_parser)

---

**Дата рефакторинга:** 10 февраля 2026  
**Версия:** 2.0.0  
**Статус:** ✅ Завершено и протестировано
